// Exercício 2: Função com tipagem explícita: Crie uma função soma(a: number, b: number): number que retorna a soma.

// Sua solução aqui
function soma(a: number, b: number) {
  return a + b;
}

console.log(soma(2, 5));
